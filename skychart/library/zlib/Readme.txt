This is to build the zlib library for Windows.
Use the standard library from your system on Linux and Mac OS.

Change to the last version in the script
run the script

(require cross-compiler mingw-w64)
